dependencies {
    def local = 'C://API/'

    compileOnly(
            files(local + 'pandaspigot-server-1.8.8-R0.1-SNAPSHOT.jar'),
            project(':common-core'),
            project(':common-bukkit')
    )
}